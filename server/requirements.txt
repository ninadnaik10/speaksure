# Production server. Acoustic inference is delegated to the Hugging Face Space
# in spaces/ -- set INFERENCE_SPACE_URL. For in-process inference instead,
# also install requirements-local.txt.

# Web layer
fastapi==0.141.1
uvicorn[standard]==0.53.0
uvicorn-worker==0.4.0
gunicorn==26.2.0
python-multipart==0.0.32
pydantic==2.13.5
pydantic-settings==2.15.0

# Data
pymongo==4.15.3

# Audio + classifier (NumPy only; see app/core/mlp.py)
numpy==2.2.6
soundfile==0.13.1

# External services
assemblyai==0.44.3
google-genai==1.43.0
gradio-client==2.7.1
