# Dev server talks to the local FastAPI process cross-origin.
# Port 8000 because macOS AirPlay Receiver occupies 5000.
# Vite serves on 6173; set CORS_ORIGINS to match in server/.env.
VITE_API_BASE=http://127.0.0.1:8000/api
